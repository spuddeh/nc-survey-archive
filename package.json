{
  "name": "nc-survey-archive",
  "version": "1.0.0",
  "private": true,
  "description": "NC Survey Archive — static Night Corp gallery. No build step; this package.json only backs the optional R2 manifest generator.",
  "type": "module",
  "scripts": {
    "gen:manifest": "node scripts/gen-manifest.mjs manifest.json > manifest.next.json",
    "delete:frames": "node scripts/delete-frames.mjs",
    "serve": "npx --yes serve@latest ."
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
